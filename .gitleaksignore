# Findings that are NOT secrets, by exact fingerprint (commit:file:rule:line).
# A fingerprint names one line in one commit, so a real secret added anywhere
# later is still found — unlike a path in .gitleaks.toml's allowlist, which
# is for files that plant credential-shaped strings on purpose.
#
# A synthetic 16-hex clone key in the landing-fetch trigger test, read as a
# generic API key. The test no longer writes it out; history keeps the line.
a4ee67ce9c0ba9751b32742a7c08853b7c2307a0:packages/connector-core/test/landing-fetch-trigger.test.ts:generic-api-key:306
